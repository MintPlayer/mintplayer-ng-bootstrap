export const environment = {
  production: true,
  // Demo is served at https://bootstrap.mintplayer.com; the API lives on a
  // separate subdomain so we POST cross-origin. apps/api Program.cs allows
  // https://bootstrap.mintplayer.com via CORS.
  apiBase: 'https://api.bootstrap.mintplayer.com',
};
