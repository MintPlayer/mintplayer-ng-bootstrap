{
  "name": "react-bootstrap-demo",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/react-bootstrap-demo/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/react-bootstrap-demo"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "dependsOn": [
        { "projects": ["api"], "target": "serve" },
        { "projects": ["mintplayer-web-components"], "target": "codegen-wc" },
        { "projects": ["mintplayer-web-components"], "target": "codegen-wc-watch" }
      ],
      "options": {
        "command": "node apps/react-bootstrap-demo/server.mjs"
      }
    },
    "preview": {
      "dependsOn": [
        "build"
      ],
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "react-bootstrap-demo:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "react-bootstrap-demo:build:development"
        },
        "production": {
          "buildTarget": "react-bootstrap-demo:build:production"
        }
      }
    },
    "serve-static": {
      "executor": "@nx/web:file-server",
      "dependsOn": [
        "build"
      ],
      "options": {
        "buildTarget": "react-bootstrap-demo:build",
        "spa": true
      }
    }
  }
}
