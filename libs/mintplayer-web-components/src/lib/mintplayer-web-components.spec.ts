import { mintplayerWebComponents } from './mintplayer-web-components';

describe('mintplayerWebComponents', () => {
  it('should work', () => {
    expect(mintplayerWebComponents()).toEqual('mintplayer-web-components');
  });
});
