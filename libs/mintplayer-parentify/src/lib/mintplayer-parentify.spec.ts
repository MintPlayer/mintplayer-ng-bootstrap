import { mintplayerParentify } from './mintplayer-parentify';

describe('mintplayerParentify', () => {
  it('should work', () => {
    expect(mintplayerParentify()).toEqual('mintplayer-parentify');
  });
});
