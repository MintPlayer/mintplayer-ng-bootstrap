import { BsLetDirective } from './let.directive';

describe('BsLetDirective', () => {
  it('should create an instance', () => {
    const directive = new BsLetDirective(null!, null!);
    expect(directive).toBeTruthy();
  });
});
