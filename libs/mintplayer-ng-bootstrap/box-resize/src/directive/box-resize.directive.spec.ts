import { BsBoxResizeDirective } from './box-resize.directive';

describe('BsBoxResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new BsBoxResizeDirective();
    expect(directive).toBeTruthy();
  });
});
