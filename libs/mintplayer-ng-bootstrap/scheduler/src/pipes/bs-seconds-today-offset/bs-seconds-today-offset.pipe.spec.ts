import { BsSecondsTodayOffsetPipe } from './bs-seconds-today-offset.pipe';

describe('BsSecondsTodayOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new BsSecondsTodayOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
