import { BsTrackByDirective } from './track-by.directive';

describe('BsTrackByDirective', () => {
  it('should create an instance', () => {
    const directive = new BsTrackByDirective(null!);
    expect(directive).toBeTruthy();
  });
});
