import { BsBlockquoteFooterDirective } from './blockquote-footer.directive';

describe('BsBlockquoteFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new BsBlockquoteFooterDirective();
    expect(directive).toBeTruthy();
  });
});
