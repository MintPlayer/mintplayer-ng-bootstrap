import { ObserveSizeDirective } from './observe-size.directive';

describe('ObserveSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveSizeDirective();
    expect(directive).toBeTruthy();
  });
});
