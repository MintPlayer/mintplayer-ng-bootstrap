import { BsCheckboxGroupValueAccessorDirective } from './checkbox-group.value-accessor.directive';

describe('BsCheckboxGroupValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCheckboxGroupValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
