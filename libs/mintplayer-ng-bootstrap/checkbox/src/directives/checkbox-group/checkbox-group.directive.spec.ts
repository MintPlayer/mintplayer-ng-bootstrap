import { BsCheckboxGroupDirective } from './checkbox-group.directive';

describe('BsCheckboxGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCheckboxGroupDirective();
    expect(directive).toBeTruthy();
  });
});
