import { BsCardBodyDirective } from './card-body.directive';

describe('BsCardBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCardBodyDirective();
    expect(directive).toBeTruthy();
  });
});
