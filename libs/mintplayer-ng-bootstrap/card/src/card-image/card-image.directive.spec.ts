import { BsCardImageDirective } from './card-image.directive';

describe('BsCardImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCardImageDirective();
    expect(directive).toBeTruthy();
  });
});
