import { BsCardFooterDirective } from './card-footer.directive';

describe('BsCardFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCardFooterDirective();
    expect(directive).toBeTruthy();
  });
});
