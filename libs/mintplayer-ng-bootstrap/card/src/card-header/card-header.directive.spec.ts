import { BsCardHeaderDirective } from './card-header.directive';

describe('BsCardHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCardHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
