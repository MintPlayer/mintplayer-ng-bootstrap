import { DisableSelectDirective } from './disable-select.directive';

describe('DisableSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableSelectDirective();
    expect(directive).toBeTruthy();
  });
});
