import { FileUploadTemplateDirective } from './file-upload-template.directive';

describe('FileUploadTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
