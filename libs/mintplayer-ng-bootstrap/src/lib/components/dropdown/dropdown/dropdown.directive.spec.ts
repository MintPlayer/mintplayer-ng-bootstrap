import { BsDropdownDirective } from './dropdown.directive';

describe('BsDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new BsDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
