import { BsDropdownMenuDirective } from './dropdown-menu.directive';

describe('BsDropdownMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new BsDropdownMenuDirective();
    expect(directive).toBeTruthy();
  });
});
