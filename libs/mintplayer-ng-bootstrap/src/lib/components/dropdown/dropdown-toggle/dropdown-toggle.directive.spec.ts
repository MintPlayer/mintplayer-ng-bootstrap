import { BsDropdownToggleDirective } from './dropdown-toggle.directive';

describe('BsDropdownToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new BsDropdownToggleDirective();
    expect(directive).toBeTruthy();
  });
});
