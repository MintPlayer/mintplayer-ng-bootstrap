import { ExpandButtonDirective } from './expand-button.directive';

describe('ExpandButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandButtonDirective();
    expect(directive).toBeTruthy();
  });
});
