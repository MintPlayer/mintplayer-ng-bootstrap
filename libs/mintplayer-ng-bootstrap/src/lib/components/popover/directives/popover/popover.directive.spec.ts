import { BsPopoverDirective } from './popover.directive';

describe('BsPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BsPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
