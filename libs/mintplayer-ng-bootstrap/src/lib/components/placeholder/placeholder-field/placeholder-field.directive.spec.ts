import { PlaceholderFieldDirective } from './placeholder-field.directive';

describe('PlaceholderFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderFieldDirective();
    expect(directive).toBeTruthy();
  });
});
