import { RowTemplateDirective } from './row-template.directive';

describe('RowTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new RowTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
