import { BsTooltipDirective } from './tooltip.directive';

describe('TooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new BsTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
