import { TabPageHeaderDirective } from './tab-page-header.directive';

describe('TabPageHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TabPageHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
