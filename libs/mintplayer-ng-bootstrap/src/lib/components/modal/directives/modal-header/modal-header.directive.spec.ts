import { BsModalHeaderDirective } from './modal-header.directive';

describe('BsModalHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new BsModalHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
