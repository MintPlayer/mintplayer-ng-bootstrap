import { BsModalDirective } from './modal.directive';

describe('BsModalDirective', () => {
  it('should create an instance', () => {
    const directive = new BsModalDirective();
    expect(directive).toBeTruthy();
  });
});
