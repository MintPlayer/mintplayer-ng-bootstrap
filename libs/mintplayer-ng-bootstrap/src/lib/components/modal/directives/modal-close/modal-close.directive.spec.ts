import { BsModalCloseDirective } from './modal-close.directive';

describe('BsModalCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new BsModalCloseDirective();
    expect(directive).toBeTruthy();
  });
});
