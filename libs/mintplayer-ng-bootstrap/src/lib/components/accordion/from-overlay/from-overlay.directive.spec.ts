import { BsFromOverlayDirective } from './from-overlay.directive';

describe('FromOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new BsFromOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
