import { BsFromOverlayIdDirective } from './from-overlay-id.directive';

describe('BsFromOverlayIdDirective', () => {
  it('should create an instance', () => {
    const directive = new BsFromOverlayIdDirective();
    expect(directive).toBeTruthy();
  });
});
