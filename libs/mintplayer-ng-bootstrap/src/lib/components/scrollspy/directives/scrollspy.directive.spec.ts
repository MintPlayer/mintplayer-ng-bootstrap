import { BsScrollspyDirective } from './scrollspy.directive';

describe('BsScrollspyDirective', () => {
  it('should create an instance', () => {
    const directive = new BsScrollspyDirective();
    expect(directive).toBeTruthy();
  });
});
