import { OffcanvasCloseDirective } from './offcanvas-close.directive';

describe('OffcanvasCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new OffcanvasCloseDirective();
    expect(directive).toBeTruthy();
  });
});
