import { BsToastDirective } from './toast.directive';

describe('BsToastDirective', () => {
  it('should create an instance', () => {
    const directive = new BsToastDirective();
    expect(directive).toBeTruthy();
  });
});
