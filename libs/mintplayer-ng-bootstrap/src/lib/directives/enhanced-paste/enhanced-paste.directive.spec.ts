import { EnhancedPasteDirective } from './enhanced-paste.directive';

describe('EnhancedPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new EnhancedPasteDirective();
    expect(directive).toBeTruthy();
  });
});
