import { BsModalBodyDirective } from './modal-body.directive';

describe('BsModalBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new BsModalBodyDirective();
    expect(directive).toBeTruthy();
  });
});
