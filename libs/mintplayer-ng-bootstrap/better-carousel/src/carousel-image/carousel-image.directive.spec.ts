import { BsCarouselImageDirective } from './carousel-image.directive';

describe('BsCarouselImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCarouselImageDirective();
    expect(directive).toBeTruthy();
  });
});
