import { BsGridColumnDirective } from './column.directive';

describe('BsGridColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new BsGridColumnDirective();
    expect(directive).toBeTruthy();
  });
});
