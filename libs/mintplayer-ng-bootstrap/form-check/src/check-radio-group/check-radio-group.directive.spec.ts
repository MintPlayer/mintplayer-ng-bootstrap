import { BsCheckRadioGroupDirective } from './check-radio-group.directive';

describe('BsCheckRadioGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCheckRadioGroupDirective();
    expect(directive).toBeTruthy();
  });
});
