import { BsNavigationLockDirective } from './navigation-lock.directive';

describe('BsNavigationLockDirective', () => {
  it('should create an instance', () => {
    const directive = new BsNavigationLockDirective();
    expect(directive).toBeTruthy();
  });
});
