import { BsShellSidebarDirective } from './shell-sidebar.directive';

describe('BsShellSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new BsShellSidebarDirective(null!, null!);
    expect(directive).toBeTruthy();
  });
});
