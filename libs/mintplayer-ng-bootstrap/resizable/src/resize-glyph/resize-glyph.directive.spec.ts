import { BsResizeGlyphDirective } from './resize-glyph.directive';

describe('BsResizeGlyphDirective', () => {
  it('should create an instance', () => {
    const directive = new BsResizeGlyphDirective();
    expect(directive).toBeTruthy();
  });
});
