import { ColorPickerValueAccessorDirective } from './color-picker-value-accessor.directive';

describe('ColorPickerValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorPickerValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
