import { BsCheckGroupDirective } from './check-group.directive';

describe('BsCheckGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new BsCheckGroupDirective();
    expect(directive).toBeTruthy();
  });
});
