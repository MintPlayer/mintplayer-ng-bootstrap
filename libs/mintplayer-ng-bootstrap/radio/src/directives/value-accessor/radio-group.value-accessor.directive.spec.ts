import { BsRadioGroupValueAccessorDirective } from './radio-group.value-accessor.directive';

describe('BsRadioGroupValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new BsRadioGroupValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
